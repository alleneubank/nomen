# Playground

Mode: Operate

Job: Draw a DNS-safe name, copy it, install the CLI.

Proof: destination plate updates from `nomen.wasm`; query string round-trips strategy, category, seed, count, and input.

Direction: Field Gazetteer. See `DESIGN.md` and `site/BRIEF.md`.
